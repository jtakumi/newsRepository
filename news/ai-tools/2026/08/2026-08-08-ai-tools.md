# 2026-08-08 AI Tools News

## 今日の要点

- **Claude Code が v2.1.223（JST 08-06 09:52）／v2.1.224（JST 08-07 13:00）** を連続公開。**セルフホスト実行（`claude self-hosted-runner`）**、**セッション間メッセージング（`SendMessage`／`ListAgents`）**、**zip アーカイブからのプラグイン導入（SHA-256 ピン留め可）**、**JWT を解釈する認証マスキング**を追加し、**1 セッション 200 サブエージェントの上限を撤廃**。`/review` は `/code-review` の別名になった。本リポジトリでは v2.1.222（JST 08-05）まで採用済みのため新規採用。
- **OpenAI Codex 0.147.0（JST 08-07 10:41、安定版）** が公開。**ポータブルなプラグイン導入**、**会話のセクション整理（永続化）**、**MCP 2026-07-28 プロトコル対応（ページング付き探索・非ブロッキング起動）**、**表示コマンド／再生履歴からのシークレット・ベアラートークンの秘匿**を追加。前回記事では 0.146.1（バグ修正）を見送っていたため、機能追加を伴う 0.147.0 を新規採用。
- **Gemini CLI v0.54.0（JST 08-06 10:35）／v0.54.4（JST 08-07 13:44、安定版）** が公開。長らく据え置きだった v0.53.1 系からの**新しい安定版マイナー**で、**`GoogleCredentialsAuthProvider` の HTTPS 強制（平文漏洩防止）**、**モデルフォールバック時のセッション ID ローテーション**、**改行コード正規化（CRLF→LF）**を含む。本リポジトリは v0.53.1 で据え置き扱いだったため新規採用。
- **OpenHands v1.11.0／v1.12.0（JST 08-08 03:01／04:33、安定版）** が公開。**実行ログ・エクスポートに 1 実行あたりの LLM コストを表示**、**タグ付きエージェントアクションで子会話を起動**、**自動化のタグ・フィルタリング**を追加。前回記事の v1.10.0（activity-log エクスポート）に続く更新で、コスト可視化とタグ運用を新規採用。
- あわせて **Qwen Code v0.21.7（JST 08-07 08:19）** が **Goals の 50 ターン上限を撤廃**し、**Kitty／Ghostty／chafa 向けのターミナル内インライン画像表示**、**`.qwen/review-context.json` によるレビュー計画のカスタマイズ**を追加。**Vercel AI SDK `ai@7.0.56`（JST 08-07 11:23）** は**動画生成リトライの冪等トークン**とコールバック／テレメトリへの**プロバイダメタデータ露出**を追加。一方、**Ollama は v0.32.6 据え置き**、**opencode v1.18.15・Cline v4.1.6／Desktop v0.0.10・LangChain の provider サブパッケージ更新**は既採用ツールの継続更新・軽微更新として扱い、詳細は調査メモに記載。

## 今日の注目

### 1. Claude Code / v2.1.223・v2.1.224

- 種別: CLI（ターミナル・コーディングエージェント／安定版）
- 関連: Anthropic / Claude / Claude Code
- 公開・更新日: v2.1.223 = 2026-08-06 00:52 UTC（JST 09:52）、v2.1.224 = 2026-08-07 04:00 UTC（JST 13:00）
- 概要: セルフホスト実行・セッション間連携・プラグイン配布・サンドボックス強化を入れた安定版更新。エージェントを自分の環境やチームで運用するための土台が拡張された。
- 何が便利か:
  - **セルフホスト実行 `claude self-hosted-runner`（v2.1.224、Team／Enterprise）**: 自前のマシン・コンテナ上で Claude Code セッションを走らせられる。
  - **セッション間メッセージング（v2.1.224、macOS／Linux）**: `SendMessage` でセッション同士がやり取りし、`ListAgents` で相手を発見できる。複数エージェントの協調に効く。
  - **zip アーカイブからのプラグイン導入（v2.1.224）**: HTTPS 経由で zip からプラグインを入れられ、SHA-256 でのピン留めに対応。配布物の完全性を担保できる。
  - **JWT を解釈する認証マスキング `decode: "jwt"`／AWS SigV4 再署名（v2.1.224）**: 認証情報の秘匿とクラウド署名の取り回しが改善。
  - **200 サブエージェント/セッションの上限を撤廃（v2.1.224）**: 並行度・深さの制限は残るが大規模なファンアウトがしやすくなった。
  - **マーケットプレイスのオーナーワイルドカード（v2.1.223）**: `strictKnownMarketplaces`／`blockedMarketplaces` で `"owner/*"` 指定が可能に。
  - **`/review` が `/code-review` の別名に（v2.1.223、PR 番号を任意指定可）**、ワークフロースクリプトのサンドボックス回避・動的 import の権限チェック・末尾スラッシュ付き deny 回避などのセキュリティ修正。
- 使いどころ:
  - 自前インフラやチーム環境で**エージェントをセルフホスト運用**したいケース。
  - サブエージェントやセッション間連携を使った**マルチエージェントの並行ワークフロー**を組みたいとき。
  - プラグインを**社内配布し、ハッシュ固定で改ざん防止**したいとき。
- 開発者にとっての価値: `npm i -g @anthropic-ai/claude-code` で入るターミナルエージェントに、運用（セルフホスト）・協調（セッション間メッセージ）・配布（アーカイブ＋SHA-256）の 3 面が加わった。特に**セッション間メッセージングとサブエージェント上限撤廃**は、複数エージェントを束ねる自動化の設計自由度を上げる。
- 注意点: セルフホスト実行は Team／Enterprise プラン向け機能。セッション間メッセージングは macOS／Linux 対応。プラグインをアーカイブ導入する際は配布元と SHA-256 の管理を徹底すること。
- 公式リンク: https://github.com/anthropics/claude-code/releases/tag/v2.1.224
- 参考リンク: https://github.com/anthropics/claude-code/releases/tag/v2.1.223 / https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md
- メモ: 本リポジトリでは v2.1.222（JST 08-05）まで採用済み。今回は v2.1.223・v2.1.224 を新規採用。

#### 使用例

```bash
# インストール／更新
npm install -g @anthropic-ai/claude-code

# バージョン確認
claude --version   # 2.1.224 を確認

# 起動
claude
```

### 2. OpenAI Codex / 0.147.0

- 種別: CLI（コーディングエージェント／安定版）
- 関連: OpenAI / Codex
- 公開・更新日: 2026-08-07 01:41 UTC（JST 10:41、安定版）。直前の 0.146.1 = 2026-08-05 15:55 UTC（JST 08-06 00:55、バグ修正）
- 概要: プラグイン導入・会話整理・MCP プロトコル更新・シークレット秘匿を入れた安定版更新。エージェントの拡張性と安全表示が中心。
- 何が便利か:
  - **ポータブルなプラグイン導入**: 拡張機能を持ち運びやすい形で追加できる。
  - **会話のセクション整理（永続化）**: 長い作業ログを区切って管理でき、後から追いやすい。
  - **MCP 2026-07-28 プロトコル対応**: ページング付きのサーバ探索と非ブロッキングなサーバ起動に対応し、MCP サーバ連携の体験が改善。
  - **シークレット・ベアラートークンの秘匿**: 表示コマンドや再生される会話履歴から機密を伏せる。ログ共有時の漏洩リスクを下げる。
  - 未知のプロジェクトに明示的な信頼付与を要求、認証情報の利用前チェック強化、日本語表示・Windows のプロセス処理・パス整合の修正。
- 使いどころ:
  - Codex をベースに**MCP サーバ群と連携**し、ページングで多数ツールを扱うエージェント構成。
  - **ログや画面を共有する運用で、機密の写り込みを防ぎたい**とき。
- 開発者にとっての価値: プラグインと MCP 更新で拡張性が上がり、シークレット秘匿・プロジェクト信頼要求といった**安全既定の強化**が入った。共有・レビュー前提でエージェントを回すチームに実利がある。
- 注意点: 0.148.0-alpha 系のプレリリースも並行しているが、安定運用は 0.147.0（安定版）を推奨。MCP 2026-07-28 対応はサーバ側の対応状況にも依存する。
- 公式リンク: https://github.com/openai/codex/releases/tag/rust-v0.147.0
- 参考リンク: https://github.com/openai/codex/releases
- メモ: 前回記事では 0.146.1 を「小規模バグ修正」で見送り。今回は機能追加を伴う安定版 0.147.0 を新規採用。

#### 使用例

```bash
# インストール／更新
npm install -g @openai/codex

# バージョン確認
codex --version   # 0.147.0

# 起動
codex
```

### 3. Gemini CLI / v0.54.0・v0.54.4

- 種別: CLI（Google のターミナル・コーディングエージェント／安定版）
- 関連: Google AI / Gemini / Gemini CLI
- 公開・更新日: v0.54.0 = 2026-08-06 01:35 UTC（JST 10:35）、v0.54.4 = 2026-08-07 04:44 UTC（JST 13:44、安定版）
- 概要: 長く v0.53.1 で据え置きだった安定版系列に、新しいマイナー v0.54 系が登場。認証・セッション・入出力整形まわりの堅牢化が中心。
- 何が便利か:
  - **`GoogleCredentialsAuthProvider` の HTTPS 強制（v0.54.0）**: 平文（クリアテキスト）での資格情報漏洩を防ぐ。
  - **モデルフォールバック時のセッション ID ローテーション（v0.54.0）**: フォールバックに伴うステートフルな API エラーを回避。
  - **改行コード正規化 CRLF→LF（v0.54.0）**: 内容提案での改行差異を吸収し、差分のブレを減らす。
  - **ファイルキーチェーンのタグ長の明示検証**、関数応答ターンの取り扱い改善、v0.54.4 で開発ブランチからの重要修正を cherry-pick。
- 使いどころ:
  - Gemini を使ったコーディング・自動化を**CLI で回し、認証やセッションの安定性を上げたい**とき。
  - Windows と Unix をまたぐチームで**改行差異による無用な差分を避けたい**とき。
- 開発者にとっての価値: `npm i -g @google/gemini-cli` で入る Google 公式ターミナルエージェントに、**新しい安定版マイナー**が来た。HTTPS 強制やセッション ID ローテーションは実運用のトラブルに直結する堅牢化で、据え置き中だった環境ほど更新価値がある。
- 注意点: v0.55.0-preview 系のプレビューも出ているが、安定運用は v0.54.4 を推奨。認証プロバイダの HTTPS 強制で、自前の非 HTTPS エンドポイントを使う構成は見直しが必要。
- 公式リンク: https://github.com/google-gemini/gemini-cli/releases/tag/v0.54.4
- 参考リンク: https://github.com/google-gemini/gemini-cli/releases/tag/v0.54.0 / https://github.com/google-gemini/gemini-cli
- メモ: 本リポジトリは v0.53.1（07-31）で据え置き扱いだった。今回 v0.54.0／v0.54.4 を新規採用。

#### 使用例

```bash
# インストール／更新
npm install -g @google/gemini-cli

# バージョン確認
gemini --version   # 0.54.4

# 起動
gemini
```

### 4. OpenHands / v1.11.0・v1.12.0

- 種別: OSS（自律型コーディングエージェント／Web UI・Agent-server）
- 関連: Devin 代替 OSS / Agent Framework
- 公開・更新日: v1.11.0 = 2026-08-07 18:01 UTC（JST 08-08 03:01）、v1.12.0 = 2026-08-07 19:33 UTC（JST 08-08 04:33、安定版）
- 概要: コスト可視化とタグ運用を強化した安定版更新。前回 v1.10.0（activity-log エクスポート）に続くマイナー更新。
- 何が便利か:
  - **1 実行あたりの LLM コストを activity ログ／エクスポートに表示（v1.11.0）**: エージェント実行の費用を実行単位で把握できる。
  - **タグ付きエージェントアクションで子会話を起動（v1.11.0）**: 作業を分岐・整理しやすくなる。
  - **自動化のタグ・フィルタリングと認識、会話タグチップ（v1.11.0）**: 多数の自動化を分類して探せる。
  - **無料の OpenHands モデルエンドポイントの明確化（v1.12.0）**、カスタマイズ・ナビゲーションの再編と Canvas UI の調整。
- 使いどころ:
  - OSS で自己ホストする自律エージェントを運用し、**実行ごとのコストを可視化して管理したい**チーム。
  - 多数の自動化を**タグで整理・検索**したいワークフロー。
- 開発者にとっての価値: 前回の activity-log エクスポートに、**実行単位のコスト可視化とタグ運用**が加わり、費用管理と大量自動化の見通しが改善。自己ホスト前提でエージェント運用をスケールさせるほど効く。
- 注意点: エージェントはファイルシステムに広くアクセスするため、**サンドボックス（Docker 実行）を推奨**。導入手順・実行イメージは版で変わるので必ず公式ドキュメントを参照。
- 公式リンク: https://github.com/All-Hands-AI/OpenHands/releases/tag/v1.12.0
- 参考リンク: https://github.com/All-Hands-AI/OpenHands/releases/tag/v1.11.0 / https://github.com/All-Hands-AI/OpenHands
- メモ: 前回記事（08-06）は v1.10.0。今回は v1.11.0 のコスト可視化・タグ運用と v1.12.0 を新規採用。

### 5. Qwen Code / v0.21.7

- 種別: CLI（OpenAI 互換プロバイダ対応コーディングエージェント／安定版）
- 関連: Qwen / OpenAI 互換プロバイダ
- 公開・更新日: 2026-08-06 23:19 UTC（JST 08-07 08:19、安定版）
- 概要: 長時間タスクの制限撤廃とターミナル体験・レビュー設定を強化した安定版更新。前回 v0.21.6（Live Voice）に続く更新。
- 何が便利か:
  - **Goals の 50 ターン上限を撤廃**: これまで打ち切られていた長いタスクを、上限を超えて再開・継続できる。
  - **ターミナル内インライン画像表示（Kitty／Ghostty／chafa）**: 対応ターミナルで画像をそのまま描画。
  - **`.qwen/review-context.json` によるレビュー計画のカスタマイズ**: 宣言的マニフェストでレビュー方針を制御できる。
  - Live Host のリリース署名修正、GitHub 起動のレビュー実行での無言ハング防止。
- 使いどころ:
  - Qwen 系や OpenAI 互換 API で、**長時間の自律タスクを途中で打ち切られずに回したい**とき。
  - リポジトリ固有のレビュー方針を**マニフェストで固定**したいチーム。
- 開発者にとっての価値: `npm i -g @qwen-code/qwen-code` で入る CLI で、**ターン上限の撤廃**は長尺タスクの実用性を直接押し上げる。レビュー・マニフェストは CI 連携でのレビュー品質の再現性に効く。
- 注意点: 利用にはプロバイダ設定（`/auth`）が必要で、挙動はモデル・プロバイダ側の対応にも依存する。インライン画像は対応ターミナルのみ。
- 公式リンク: https://github.com/QwenLM/qwen-code/releases/tag/v0.21.7
- 参考リンク: https://github.com/QwenLM/qwen-code
- メモ: 本リポジトリは v0.21.6（08-06 記事）まで採用済み。今回は v0.21.7 を新規採用。

### 6. Vercel AI SDK / ai@7.0.56

- 種別: OSS（TypeScript の AI アプリ SDK）
- 関連: Vercel / AI SDK
- 公開・更新日: 2026-08-07 02:23 UTC（JST 11:23）。直後に ai@7.0.57 = 2026-08-07 18:06 UTC（JST 08-08 03:06、依存更新）
- 概要: リトライの安全性と可観測性を強化したコア `ai` パッケージの更新。課金の二重発生防止とテレメトリ拡充が中心。
- 何が便利か:
  - **動画生成リトライの冪等トークン（ヘッダ転送）**: リトライ時の**課金対象の二重生成を防ぐ**。
  - **言語モデル呼び出し終了コールバック／テレメトリスパンにプロバイダメタデータを露出**: 監視・デバッグ時に呼び出し元の情報を取りやすい。
  - **ストリーム再開処理の改善**: キャンセル後の更新重複を防ぎ、レースコンディションを回避。
- 使いどころ:
  - TypeScript でチャットや生成機能を実装し、**リトライで二重課金させたくない**アプリ。
  - モデル呼び出しの**メタデータをテレメトリに載せて観測性を上げたい**とき。
- 開発者にとっての価値: `npm i ai` で使う定番 SDK の**信頼性と可観測性**の底上げ。特に冪等トークンは、リトライを持つ本番アプリで**実費の無駄を防ぐ**実利がある。
- 注意点: 動画生成の冪等性はプロバイダ側のヘッダ対応に依存する。7.0.57 は依存更新中心。メジャー系列（6.x／7.x）が並行しているため、利用中の系列に合わせて更新すること。
- 公式リンク: https://github.com/vercel/ai/releases/tag/ai%407.0.56
- 参考リンク: https://github.com/vercel/ai/releases
- メモ: 本リポジトリでコア `ai` パッケージの機能更新を採用するのは今回が初。JST 08-07 の ai@7.0.56 を新規採用。

## 今日試すならこれ

### Claude Code / v2.1.224

選定理由:

- `npm install -g @anthropic-ai/claude-code` の 1 コマンドで更新でき、**今日すぐ試せる**（導入しやすさ）。
- **セッション間メッセージング（`SendMessage`／`ListAgents`）とサブエージェント上限の撤廃**で、マルチエージェントの並行ワークフローを組みやすくなった（開発者価値）。
- **アーカイブからのプラグイン導入＋SHA-256 ピン留め**で、社内配布物の改ざん防止まで含めて運用に落とせる（実用性・信頼性）。

最初に試すこと:

```bash
# 更新してバージョン確認
npm install -g @anthropic-ai/claude-code
claude --version   # 2.1.224

# 起動して、別セッションと連携する運用を試す
claude
# セッション内で ListAgents / SendMessage を使って他セッションへメッセージを送る
```

## 調査メモ

- 調査対象期間: 2026-08-06〜2026-08-08（JST）。24〜48 時間ウィンドウで、一次情報（GitHub Releases／各社公式リリースノート）の確定リリースを中心に採用。前回記事（〜08-06）採用分との重複を除外。
- 主な確認先:
  - GitHub Releases: anthropics/claude-code, openai/codex, google-gemini/gemini-cli, All-Hands-AI/OpenHands, QwenLM/qwen-code, vercel/ai, BerriAI/litellm, ollama/ollama, cline/cline, sst/opencode, Aider-AI/aider, langchain-ai/langchain, RooCodeInc/Roo-Code
  - 公式リリースノート／ドキュメント: anthropics/claude-code CHANGELOG.md
- 採用しなかった主な理由:
  - **Ollama**: v0.32.6（JST 08-06）が最新のまま据え置きで、ウィンドウ内の新規安定版なし。前回記事で採用済みのため見送り。
  - **opencode（v1.18.15、Aug 7）**: メッセージ時系列の整合修正、Web UI の blob 添付処理、デスクトップのロケール拡充など。**opencode は 08-02・08-06 で採用済み**の継続更新のため見送り。
  - **Cline（v4.1.6・CLI v3.0.51・SDK v0.0.71・Desktop v0.0.10、Aug 6〜7）**: リモート MCP サーバの OAuth 認証（Desktop v0.0.10）やプロバイダ横断の reasoning 設定などがあるが、**Cline は 08-01・08-02・08-06 で継続採用済み**のため今回は見送り（リモート MCP OAuth は次回まとまれば再採用候補）。
  - **LiteLLM**: ウィンドウ内は v1.97.0-dev 系のプレリリース（dev.1／dev.2）中心で、安定版の新規リリースなし。前回 v1.95.0 を採用済みのため、開発版は見送り。
  - **LangChain**: ウィンドウ内は langchain-openai 1.4.2（`ContextWindowExceededError` 処理など）と langchain-anthropic 1.5.4（前回記事の調査メモで言及済み）の provider サブパッケージ更新のみで、`langchain`／`langchain-core` 本体の新規リリースなし。用途が限定的なため見送り。
  - **Vercel AI SDK ai@7.0.57／ai@6.0.246**: 依存更新・保守中心のため、機能追加のある ai@7.0.56 のみ採用。
  - **Aider（Aider-AI/aider）**: 最新の正式リリースは v0.86.0（2025-08）でウィンドウ外。Aug 2026 のリリースなし。
  - **Roo Code（RooCodeInc/Roo-Code）**: リポジトリが 2026-05-15 にアーカイブ済み（読み取り専用）。ウィンドウ内のリリースなし。
  - **Cursor**: 公式 changelog（cursor.com）がネットワーク egress ポリシーによりアクセス不可で、一次情報を確認できなかったため信頼性を優先して見送り。
